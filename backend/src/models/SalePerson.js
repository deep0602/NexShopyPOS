class SalePerson {
    constructor(id, name, commission, createdAt) {
      this.id = id;
      this.name = name;
      this.commission = commission; 
      this.createdAt = createdAt;
    }
  }
  
  module.exports = SalePerson;